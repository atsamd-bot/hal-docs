initSidebarItems({"mod":[["v1","Version 1 of the GPIO moduleThis module is a compatibility shim that allows existing code to use the new v2 module. This API will eventually be deprecated and removed. The remaining documentation in this module comes from the prior implementation of the GPIO module."],["v2","Version 2 of the GPIO moduleThe new API provides two different submodules, [pin] and [dynpin], representing two different ways to handle GPIO pins. The default, [pin], is a type-level API that tracks the state of each pin at compile-time. The alternative, [dynpin] is a type-erased, value-level API that tracks the state of each pin at run-time."]]});